#include "holberton.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int _abs(int r)
{

	int x;
	x = -1;
	
	
	if (r > 0)
	{
		return (r);
	}
	else
	{
		r = r * x;
		return (r);
	}
}
 
  
