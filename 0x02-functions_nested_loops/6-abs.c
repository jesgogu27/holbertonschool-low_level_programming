#include "holberton.h"
/**
 * _abs - Computes the abrolute value of an integer
 * @r: int r
 * Return: r (Success)
 */
int _abs(int r)
{
int x;
x = -1;
if (r > 0)
{
return (r);
}
else
{
r = r *x;
return (r);
}
}
