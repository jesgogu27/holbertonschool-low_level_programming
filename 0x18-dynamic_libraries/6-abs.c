#include "holberton.h"

/**
 * _abs - Entry point
 * @r: Number to change
 * Return: Always 0 (Success)
 */

int _abs(int r)
{

	int x;

	x = -1;


	if (r > 0)
	{
		return (r);
	}
	else
	{
		r = r * x;
		return (r);
	}
}

