#include "holberton."

/**
 * print_line - that prints 10 times the numbers, from 0 to 14
 *@c: integer  
 *Return: n
 */

void print_line(int n)
{

  int a, b;

  a = n;
  
for (b = 0; b <= a; b++)
{
if (b > 9)
{
_putchar(b / 10 + '0');
}
_putchar(b % 10 + '0');
}
