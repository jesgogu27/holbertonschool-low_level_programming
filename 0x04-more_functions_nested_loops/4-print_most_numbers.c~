#include "holberton.h"

/**
 * print_numbers - checks for a digit (0 through 9)
 * Return: always (0)
 */

void print_most_numbers(void)
{

int a;

for (a = 0; a < 10; a++)
{
if((a != 2) && (a != 4))
{
_putchar(a + '0');
}
}
_putchar('\n');
}
