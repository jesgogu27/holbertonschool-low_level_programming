#include "holberton.h"

/**
 *_strlen - returns the length of a string
 *@s: string received
 *
 *Return: the length of a string
 */

int _strlen(char *s)
{

  int nu;

  nu =  strlen(*s);

  return (nu);
}

